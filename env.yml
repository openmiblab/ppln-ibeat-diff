name: diff
channels:
  - pytorch
  - nvidia  # Add this if you have an NVIDIA GPU
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pandas
  - tqdm
  - pip

  - pip:
      - -e .    # This installs the pipeline from source
      - miblab   # Any packages that are not on conda need to be listed here
      - miblab_data