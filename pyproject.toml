[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ppln-ibeat-diff"
version = "0.0.0"
dependencies = [
  'pandas',
  'miblab',
  'miblab_data',
]

[tool.setuptools.packages.find]
where = ["src"]